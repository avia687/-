{
  "name": "dropship-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Multi-tenant dropshipping SaaS — runnable foundation",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run -w apps/api build && npm run -w apps/web build",
    "lint": "npm run -w apps/api lint",
    "test": "npm run -w apps/api test"
  },
  "engines": {
    "node": ">=20"
  }
}
